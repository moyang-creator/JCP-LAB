{
  "name": "jcp-lab",
  "version": "1.0.0",
  "scripts": {
    "deploy:all": "npx hardhat run scripts/deploy-all.js --network orbitDevnet",
    "deploy": "npx hardhat run scripts/deploy.js --network orbitDevnet"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.6"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "hardhat": "^2.19.0",
    "dotenv": "^16.3.1"
  }
}